{
  "name": "cssanim",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "browserify index.js -o app.js -t [ babelify --optional runtime ]",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "watchify index.js -o app.js -t [ babelify --optional runtime ] -v"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "angular": "^1.4.0",
    "angular-animate": "^1.4.0"
  },
  "devDependencies": {
    "babelify": "^6.1.2",
    "browserify": "^10.2.1",
    "watchify": "^3.2.1"
  }
}
